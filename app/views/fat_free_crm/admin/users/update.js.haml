- id = dom_id(@user, 'fat_free_crm')

- if @user.errors.empty?
  $('##{id}').replaceWith('#{ j render(partial: "user", collection: [ @user ]) }')
  $('##{id}').effect("highlight", { duration:1000 });
- else
  $('##{id}').html('#{j render(partial: "edit") }')
  $('#user_username').focus()
