= styles_for :tag

.title_tools
  = link_to_inline(:create_tag, new_admin_tag_path, text: t(:create_tag))

.title
  %span#create_tag_title #{t :tags}
  = image_tag("fat_free_crm/loading.gif", size: :thumb, id: "loading", style: "display: none;")
.remote#create_tag{ hidden }

.list#tags
  - if @tags.any?
    = render partial: "fat_free_crm/admin/tags/tag", collection: @tags
  - else
    = render "fat_free_crm/shared/empty"

#export= render "fat_free_crm/shared/export"
