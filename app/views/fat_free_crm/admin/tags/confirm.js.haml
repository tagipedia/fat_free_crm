- id = dom_id(@tag, :confirm_fat_free_crm)

if ($('##{id}').size() > 0) {
crm.flick('#{id}', 'remove');
} else {
$('##{dom_id(@tag, 'fat_free_crm')}').prepend('#{ j (render partial: "confirm") }');
}
