- @versions = FatFreeCrm::Version.recent_for_user(current_fat_free_crm_user)
.panel#recently
  .caption #{t :recent_items}
  - if @versions.present?
    = render partial: "fat_free_crm/shared/recent", collection: @versions
  - else
    %div #{t :no_recent_items}
