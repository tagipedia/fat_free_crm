$('##{dom_id(@attachment, 'fat_free_crm')}').slideUp(250);

- if called_from_landing_page?(:accounts)
  = refresh_sidebar_for(:accounts, :show)
- elsif called_from_landing_page?(:campaigns)
  = refresh_sidebar_for(:campaigns, :show)
