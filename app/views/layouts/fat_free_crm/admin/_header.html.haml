#header
  #welcome
    %span
      #{t :welcome},
      = current_fat_free_crm_user.name
    %span> !&nbsp;&nbsp;

    = link_to(t(:quick_find), "#", id: "jumper") << " |"
    = link_to(t(:profile), profile_path) << " |"
    = link_to(t(:back_to_crm), root_path) << " |"
    = link_to(t(:logout), destroy_fat_free_crm_user_session_path, method: :delete)
    = render "/layouts/fat_free_crm/jumpbox"
  %h3
    = link_to(t(:crm_admin_page), admin_path)
